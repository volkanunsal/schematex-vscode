# UML Use Case: Library Borrowing

A minimal use case diagram for a library member borrowing a book.

```schematex
usecase
system: "Library"

actor: Member
usecase: "Borrow Book" as Borrow

Member -- Borrow
```
